# Models

Store local pretrained weights and exported model artifacts here.

Examples:

```text
models/unet.torchscript
models/classifier.torchscript
models/*.onnx
```

Large model files are ignored by git.
